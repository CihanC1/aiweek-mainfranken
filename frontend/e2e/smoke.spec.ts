import{expect,test}from'@playwright/test';
const events=[{id:'30000000-0000-0000-0000-000000000001',title:'Java & Cloud Meetup',organizer:'Mainfranken JUG',description:'Modern Java und Cloud-native Systeme',tags:['java','cloud'],eventType:'MEETUP',startAt:'2030-07-14T18:00:00+02:00',locationName:'Tech Hub',city:'Würzburg',attendanceMode:'OFFLINE',sourceUrl:'https://example.org',sourceName:'Demo',status:'ACTIVE'},{id:'30000000-0000-0000-0000-000000000002',title:'Applied AI Workshop',organizer:'AI Network Franken',description:'Hands-on AI Engineering',tags:['ai','startup'],eventType:'WORKSHOP',startAt:'2030-07-20T18:00:00+02:00',locationName:'Innovation Lab',city:'Nürnberg',attendanceMode:'HYBRID',sourceUrl:'https://example.org',sourceName:'Demo',status:'ACTIVE'}];
test.beforeEach(async({page})=>{await page.route('**/api/auth/me',r=>r.fulfill({status:401,contentType:'application/json',body:'{"message":"Anmeldung erforderlich"}'}));await page.route('**/api/events',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify(events)}))});
test('home and mobile navigation stay usable',async({page},testInfo)=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Finde das Event/})).toBeVisible();await expect(page.getByRole('link',{name:/MainFrankenIT Startseite/})).toBeVisible();await expect(page.getByText('Java & Cloud Meetup').first()).toBeVisible();const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false);await page.screenshot({path:testInfo.outputPath('home.png'),fullPage:true});});
test('login validates short passwords',async({page})=>{await page.goto('/login');await page.getByLabel('E-Mail').fill('ada@example.de');await page.getByLabel('Passwort').fill('short');await page.getByRole('button',{name:'Anmelden'}).click();await expect(page.getByRole('alert')).toContainText('mindestens 8')});
